{
  "tasks": {
    "format": "deno fmt --config deno.json",
    "lint": "deno lint --fix --config deno.json",
    "check": "deno check --config deno.json **/*.ts",
    "dev": "deno run --allow-read --allow-write --allow-run main.ts"
  },
  "lint": {
    "rules": {
      "tags": [
        "recommended",
        "jsr"
      ]
    }
  },
  "fmt": {
    "semiColons": false,
    "singleQuote": true
  }
}